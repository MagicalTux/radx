# Radix Tree-like on-disk database for Go

This is something similar to a radix tree, with some changes.

Block size is fixed to make reads faster (block alignement).

This is really optimized for reads.
